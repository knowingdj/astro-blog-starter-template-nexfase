---
const site = {
  name: "Nexfase",
  tagline: "Supply chain solutions for food and beverage businesses",
  email: "info@nexfase.com.au",
  phone: "+61 400 000 000",
};
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{site.name}</title>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap');

      :root {
        --max-width: 900px;
        --primary-color: #00d084;
        --bg-color: #121212;
        --section-bg: #1f1f1f;
        --text-color: #ffffff;
        --subtle-text: #b3b3b3;
        --line-height: 1.6;
        --radius: 12px;
        --shadow: 0 4px 12px rgba(0,0,0,0.6);
      }

      body {
        font-family: 'Inter', system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif;
        margin: 0;
        padding: 0;
        color: var(--text-color);
        line-height: var(--line-height);
        background-color: var(--bg-color);
      }

      .container {
        max-width: var(--max-width);
        margin: 0 auto;
        padding: 64px 24px 100px;
      }

      h1 {
        font-size: 3rem;
        margin-bottom: 12px;
        font-weight: 700;
      }

      h2 {
        font-size: 1.8rem;
        margin-bottom: 16px;
        font-weight: 600;
      }

      p, li {
        color: var(--subtle-text);
        font-size: 1rem;
      }

      .hero {
        margin-top: 40px;
        text-align: center;
      }

      .hero img {
        max-width: 160px;
        margin-bottom: 24px;
        border-radius: 16px;
        box-shadow: var(--shadow);
      }

      .tagline {
        font-size: 1.3rem;
        color: var(--primary-color);
        margin: 0 auto;
        max-width: 600px;
      }

      section {
        margin-top: 60px;
        background: var(--section-bg);
        padding: 40px 32px;
        border-radius: var(--radius);
        box-shadow: var(--shadow);
      }

      ul {
        padding-left: 20px;
      }

      li {
        margin-bottom: 12px;
      }

      footer {
        margin-top: 80px;
        padding-top: 32px;
        border-top: 1px solid #333;
        text-align: center;
        color: var(--subtle-text);
      }

      a {
        color: var(--primary-color);
        text-decoration: none;
      }

      a:hover {
        text-decoration: underline;
      }

      @media (max-width: 600px) {
        .container { padding: 32px 16px; }
        h1 { font-size: 2.2rem; }
        h2 { font-size: 1.5rem; }
      }


            .contact-form {
        display: flex;
        flex-direction: column;
        gap: 20px;
        margin-top: 20px;
      }

      .contact-form label {
        color: var(--text-color);
        font-weight: 600;
        display: flex;
        flex-direction: column;
        gap: 8px;
      }

      .contact-form input,
      .contact-form textarea {
        padding: 12px 14px;
        background: #2a2a2a;
        border: 1px solid #333;
        border-radius: 10px;
        color: var(--text-color);
        font-size: 1rem;
        font-family: inherit;
      }

      .contact-form input:focus,
      .contact-form textarea:focus {
        outline: none;
        border-color: var(--primary-color);
        box-shadow: 0 0 0 2px rgba(0, 208, 132, 0.3);
      }

      .contact-form button {
        padding: 14px 20px;
        background: var(--primary-color);
        color: #000;
        font-weight: 600;
        font-size: 1.1rem;
        border-radius: 12px;
        border: none;
        cursor: pointer;
        transition: 0.2s ease;
      }

      .contact-form button:hover {
        opacity: 0.85;
        transform: translateY(-1px);
      }

      .contact-form button:active {
        transform: translateY(0);
      }

    </style>
  </head>
  <body>
    <div class="container">
      <!-- HERO -->
      <header class="hero">
        <img src="/logo.png" alt="Nexfase Logo" style="display: block; margin: 0 auto; margin-bottom: 3rem;" />
        <p class="tagline">{site.tagline}</p>
      </header>
      <!-- ABOUT -->
      <section>
        <h2>About</h2>
        <p>
          Nexfase helps food and beverage businesses keep their systems running without interruption.
          With deep experience building and supporting ERP and supplier chain platforms,
          the focus is on stability, fast support, and smooth daily operations.
        </p>
        <p>
          Think of us as your behind the scenes partner making sure your ordering, procurement, and operational systems never skip a beat.
        </p>
      </section>

      <!-- WHAT WE DO -->
      <section>
        <h2>What We Do</h2>
        <ul>
          <li><strong>System Maintenance:</strong> Keep your ERP and operational systems stable with proactive monitoring, updates, and fixes.</li>
          <li><strong>IT Support:</strong> Clear, responsive help for day-to-day technical issues.</li>
          <li><strong>Supply Chain Platform Expertise:</strong> Hands-on experience with ordering, inventory, procurement, and supplier integrations in the food and restaurant sector.</li>
          <li><strong>Custom Enhancements:</strong> Workflow improvements, small builds, and integration updates tailored to your operations.</li>
        </ul>
      </section>

      <!-- WHY NEXFASE -->
      <section>
        <h2>Why Nexfase</h2>
        <ul>
          <li>Deep experience in food, beverage, and restaurant operations.</li>
          <li>Practical understanding of franchise and multi-site workflows.</li>
          <li>Strong integration background with supplier APIs and procurement systems</li>
          <li>Fast response and ongoing support you can rely on.</li>
        </ul>
      </section>

      <!-- REACH OUT -->
      <section>
        <h2>Reach Out</h2>  
        <form action="/contact" method="POST" class="contact-form">
            <div class="form-row">
              <label for="name">Name</label>
              <input id="name" type="text" name="name" required />
            </div>

            <div class="form-row">
              <label for="email">Email</label>
              <input id="email" type="email" name="email" required />
            </div>

            <div class="form-row">
              <label for="message">Message</label>
              <textarea id="message" name="message" rows="5" required></textarea>
            </div>

            <button type="submit">Send Message</button>
          </form>

      </section>

      <!-- Contact -->
      <footer>
        
        <p><a href="mailto:{site.email}">{site.email}</a> </p>
        <p>Sydney, Australia</p>
      </footer>
    </div>
  </body>
</html>
